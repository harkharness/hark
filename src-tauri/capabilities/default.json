{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Mother window + HQ + per-project windows",
  "windows": ["main", "hq", "proj-*"],
  "permissions": ["core:default"]
}
