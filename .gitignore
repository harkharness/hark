# Rust
target/
**/*.rs.bk

# Node / frontend (Tauri UI, added in M2)
node_modules/
dist/

# Local data: NEVER commit. The index and recordings may contain
# fragments of your private Claude Code history.
*.db
*.sqlite
*.wav
*.aiff
recordings/
models/
*.bin

# OS
.DS_Store

# Spike scratch output

# Per-project agent memory (journal/state/briefs) - never commit
.vox/
.hark/

# Generated mobile icon sets (desktop app only)
src-tauri/icons/android/
src-tauri/icons/ios/

# Design canvases: os artboards (.dc.html) e o layout SÃO fonte; o arquivo
# publicado é 2 MB de editor empacotado, regenerável com um comando.

# Dev-server entry for the browser pane: machine-local.
.claude/launch.json

# Design canvases, spikes and private notes live in private repos (the
# maintainers' workspace), never here.
.design/
spikes/
